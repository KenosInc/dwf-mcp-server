{
  "mcpServers": {
    "dwf": {
      "command": "docker",
      "args": [
        "run", "-i", "--rm",
        "--device=/dev/bus/usb",
        "dwf-mcp-server",
        "dwf-mcp-server"
      ]
    }
  }
}
